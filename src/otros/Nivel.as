package otros 
{
	/**
	 * Esta clase cargará el XML del nivel y almacenará todos sus datos.
	 * Mediante funciones se leerán desde otros sitios estos datos.
	 */
	public class Nivel 
	{
		
		public function Nivel() 
		{
			/*xmlData = new XML(new Assets.nivelDePrueba());
			
			trace(xmlData.arrow);
			
			attr = xmlData.arrow.attributes();
			
			for each (var tipo:XML in attr)
			{
				if (tipo.name() == "type")
				{
					trace(tipo);
				}
			}
			
			trace("DFSDFASF");
			
			trace(xmlData.caract.attribute("puntos"));*/
			
			
		}
		
	}

}